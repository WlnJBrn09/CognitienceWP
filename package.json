{
  "name": "cognitience-wp",
  "version": "2.0.0",
  "description": "Cognitience WP — local-first word processor with liquid glass UI and Rust backend",
  "main": "electron/main.js",
  "author": "Cognitience",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "node scripts/test-liquid-glass.js && node scripts/test-pdf-helpers.js",
    "test:pdf": "node scripts/test-pdf-helpers.js && node scripts/test-pdf-open.js",
    "test:glass": "node scripts/test-liquid-glass.js && node scripts/verify-glass-structure.js",
    "backend": "cargo run --release",
    "backend:build": "cargo build --release",
    "start": "cargo run",
    "electron": "electron .",
    "electron:dev": "cargo build --release && electron .",
    "icon": "node scripts/make-icon.js",
    "dist": "cargo build --release && node scripts/make-icon.js && electron-builder --config electron-builder.config.js --win portable",
    "dist:dir": "cargo build --release && node scripts/make-icon.js && electron-builder --config electron-builder.config.js --win dir",
    "dist:mac": "cargo build --release && node scripts/make-icon.js && CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --config electron-builder.config.js --mac zip --arm64",
    "dist:mac:x64": "rustup target add x86_64-apple-darwin && cargo build --release --target x86_64-apple-darwin && node scripts/make-icon.js && COG_RUST_TARGET=x86_64-apple-darwin CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --config electron-builder.config.js --mac zip --x64"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/WlnJBrn09/CognitienceWP.git"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "png-to-ico": "^2.1.8"
  },
  "allowScripts": {
    "electron@33.2.1": true
  }
}
